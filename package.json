{
  "name": "fluxly-ccs",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "@types/bun": "latest",
    "@types/node": "latest"
  }
}